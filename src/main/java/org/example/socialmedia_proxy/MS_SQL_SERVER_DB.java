package org.example.socialmedia_proxy;

public class MS_SQL_SERVER_DB extends Database {
    public MS_SQL_SERVER_DB(String url, String username, String password) {
        super(url, username, password);
    }
}
